import React from "react";

const Plot = ({ equation }) => {
  return (
    <div id="plot" style={{ width: "100%", height: "400px" }}>
      {}
      <p>Graph plotting for the equation is not currently available.</p>
    </div>
  );
};

export default Plot;
